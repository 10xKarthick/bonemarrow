{
  "name": "bonemarrow",
  "version": "0.1.0",
  "private": false,
  "scripts": {
    "build": "esbuild src/index.ts --bundle --format=iife --global-name=bone --target=es2019 --outfile=dist/bonemarrow.js",
    "publish": "esbuild src/index.ts --bundle --format=iife --global-name=bone --target=es2019 --minify --outfile=dist/bonemarrow.min.js"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "esbuild": "^0.27.2"
  }
}
